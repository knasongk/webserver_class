const hello = (nameOfUser1, nameOfUser2) => {
	return `Hello ${nameOfUser1} and ${nameOfUser2}`;
};

export const goodbye = (nameOfUser1, nameOfUser2) => {
	return `Goodbye ${nameOfUser1} and ${nameOfUser2}`;
}

export const myMult = (x,y) => {
	return x*y;
}

export default hello;
